// Media Scan API

// Collects redis document database of movies / shows in terms of id -> path of head folder
// Provides:
// - Request scan of media
// -> mediaId | path | movie/show (database)

// /movies
// Request all movie ids

// /shows
// Request all show ids

// media/mediaId
// Request metadata for mediaId
// -> path | movie/show | ?

export const mediaScan = (mediaFolder: string) => {

}

export const movieScan = (movieFolder: string) => {

}

export const showScan = (showFolder: string) => {
}